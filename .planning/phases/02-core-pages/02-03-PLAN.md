---
phase: 02-core-pages
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - sections/haze-value-props.liquid
  - assets/haze-value-props.css
  - assets/haze-icon-ritual.svg
  - assets/haze-icon-leaf.svg
  - assets/haze-icon-gift.svg
  - sections/haze-social-proof.liquid
  - assets/haze-social-proof.css
  - sections/haze-bottom-cta.liquid
  - assets/haze-bottom-cta.css
  - templates/index.json
autonomous: true
requirements:
  - HOME-03
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "Three value prop blocks render with icon, heading, and body text in a 3-column grid on desktop and stacked on mobile"
    - "The social proof section outputs no HTML when show_section is false (default)"
    - "Toggling show_section to true in Customize reveals 3 testimonial cards with star ratings"
    - "The bottom CTA section uses blush background (color-scheme-2) with a terracotta CTA button linking to /products/hair-ritual-kit"
    - "The homepage template renders all 5 Haze sections in order: hero, product grid, value props, social proof, bottom CTA"
    - "All sections are visible at 375px mobile width with correct spacing and no horizontal scroll"
  artifacts:
    - path: "sections/haze-value-props.liquid"
      provides: "3-block value prop section with schema"
      contains: "{% schema %}"
    - path: "assets/haze-value-props.css"
      provides: "3-column responsive grid for value props"
      contains: ".haze-value-props"
    - path: "assets/haze-icon-ritual.svg"
      provides: "Value prop icon 1 — ritual/circle motif"
      contains: "<svg"
    - path: "assets/haze-icon-leaf.svg"
      provides: "Value prop icon 2 — leaf/natural motif"
      contains: "<svg"
    - path: "assets/haze-icon-gift.svg"
      provides: "Value prop icon 3 — gift box motif"
      contains: "<svg"
    - path: "sections/haze-social-proof.liquid"
      provides: "Hidden testimonial section with show_section toggle"
      contains: "show_section"
    - path: "assets/haze-social-proof.css"
      provides: "Testimonial card styling"
      contains: ".haze-social-proof"
    - path: "sections/haze-bottom-cta.liquid"
      provides: "Compact CTA section with blush background"
      contains: "{% schema %}"
    - path: "assets/haze-bottom-cta.css"
      provides: "Bottom CTA styling with blush background"
      contains: ".haze-bottom-cta"
    - path: "templates/index.json"
      provides: "Homepage template wiring all 5 Haze sections"
      contains: "haze-hero"
  key_links:
    - from: "sections/haze-value-props.liquid"
      to: "assets/haze-value-props.css"
      via: "stylesheet_tag"
      pattern: "haze-value-props.css.*asset_url.*stylesheet_tag"
    - from: "sections/haze-social-proof.liquid"
      to: "assets/haze-social-proof.css"
      via: "stylesheet_tag"
      pattern: "haze-social-proof.css.*asset_url.*stylesheet_tag"
    - from: "sections/haze-bottom-cta.liquid"
      to: "assets/haze-bottom-cta.css"
      via: "stylesheet_tag"
      pattern: "haze-bottom-cta.css.*asset_url.*stylesheet_tag"
    - from: "templates/index.json"
      to: "sections/haze-hero.liquid"
      via: "section type reference"
      pattern: "haze-hero"
    - from: "templates/index.json"
      to: "sections/haze-product-grid.liquid"
      via: "section type reference"
      pattern: "haze-product-grid"
    - from: "templates/index.json"
      to: "sections/haze-value-props.liquid"
      via: "section type reference"
      pattern: "haze-value-props"
    - from: "templates/index.json"
      to: "sections/haze-social-proof.liquid"
      via: "section type reference"
      pattern: "haze-social-proof"
    - from: "templates/index.json"
      to: "sections/haze-bottom-cta.liquid"
      via: "section type reference"
      pattern: "haze-bottom-cta"
---

<objective>
Create the value props, social proof, and bottom CTA sections, then wire all five Haze sections into the homepage template.

Purpose: These three sections complete the homepage brand experience below the product grid. Value props communicate the kit's selling points, social proof (hidden by default) is a future activation point for real reviews, and the bottom CTA gives visitors a final nudge toward purchasing. The `templates/index.json` update replaces Dawn's default homepage with the complete Haze layout.

Output: 3 new section/CSS pairs, 3 SVG icons, and an updated `templates/index.json` that renders all 5 homepage sections in the correct order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages/02-CONTEXT.md
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-UI-SPEC.md
@assets/haze-tokens.css

<interfaces>
<!-- Design tokens from haze-tokens.css (loaded globally in theme.liquid) -->
:root {
  --haze-color-terracotta: #C4714A;
  --haze-color-cream:      #F7F3EE;
  --haze-color-blush:      #E8D5CE;
  --haze-color-dark:       #2C2420;
  --haze-space-xs:  0.4rem;   /* 4px  */
  --haze-space-sm:  0.8rem;   /* 8px  */
  --haze-space-md:  1.6rem;   /* 16px */
  --haze-space-lg:  3.2rem;   /* 32px */
  --haze-space-xl:  6.4rem;   /* 64px */
  --haze-radius-button: 2px;
  --haze-radius-card:   4px;
  --font-heading-family: 'Cormorant Garamond', Georgia, serif !important;
  --font-body-family:    'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif !important;
}

<!-- Sections created by Plan 01 and Plan 02 (must exist before index.json references them) -->
- sections/haze-hero.liquid (type: "haze-hero")
- sections/haze-product-grid.liquid (type: "haze-product-grid")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create value props section with SVG icons</name>
  <files>sections/haze-value-props.liquid, assets/haze-value-props.css, assets/haze-icon-ritual.svg, assets/haze-icon-leaf.svg, assets/haze-icon-gift.svg</files>
  <read_first>
    - assets/haze-tokens.css (design tokens — spacing, colors, radius, fonts)
    - .planning/phases/02-core-pages/02-UI-SPEC.md (value prop component spec: layout, typography, spacing, icon details)
    - .planning/phases/02-core-pages/02-RESEARCH.md (Pattern 5: section-scoped CSS, complete haze-value-props.liquid code example)
  </read_first>
  <action>
**Create 3 SVG icon files** in `assets/`:

Each icon is 40x40px, single-color (uses `currentColor` for CSS colorability), simple line art style consistent with the warm editorial brand.

**`assets/haze-icon-ritual.svg`** — a circular motif representing a daily ritual (e.g., a circle with radiating lines or a simple mandala):
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
  <circle cx="20" cy="20" r="12"/>
  <circle cx="20" cy="20" r="4"/>
  <line x1="20" y1="2" x2="20" y2="8"/>
  <line x1="20" y1="32" x2="20" y2="38"/>
  <line x1="2" y1="20" x2="8" y2="20"/>
  <line x1="32" y1="20" x2="38" y2="20"/>
</svg>
```

**`assets/haze-icon-leaf.svg`** — a single leaf representing natural/thoughtful sourcing:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
  <path d="M10 34 C10 34 8 16 20 8 C32 16 30 34 30 34"/>
  <path d="M20 8 L20 30"/>
  <path d="M14 20 L20 16"/>
  <path d="M26 20 L20 16"/>
  <path d="M14 26 L20 22"/>
  <path d="M26 26 L20 22"/>
</svg>
```

**`assets/haze-icon-gift.svg`** — a gift box with ribbon:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
  <rect x="5" y="16" width="30" height="20" rx="2"/>
  <rect x="3" y="10" width="34" height="6" rx="1"/>
  <line x1="20" y1="10" x2="20" y2="36"/>
  <path d="M20 10 C20 10 14 4 10 6 C6 8 10 10 12 10"/>
  <path d="M20 10 C20 10 26 4 30 6 C34 8 30 10 28 10"/>
</svg>
```

**Create `sections/haze-value-props.liquid`** — 3-block value prop section (per D-15, D-16, D-17).

Full section structure:
```liquid
{{ 'haze-value-props.css' | asset_url | stylesheet_tag }}

<section id="haze-value-props-{{ section.id }}" class="haze-value-props">
  <div class="haze-value-props__grid">
    {%- for block in section.blocks -%}
      {%- if block.type == 'prop' -%}
        <div class="haze-value-props__block" {{ block.shopify_attributes }}>
          {%- if block.settings.icon != blank -%}
            <div class="haze-value-props__icon" aria-hidden="true">
              <img
                src="{{ block.settings.icon | asset_url }}"
                alt=""
                width="40"
                height="40"
                loading="lazy"
              >
            </div>
          {%- endif -%}
          <h3 class="haze-value-props__heading">{{ block.settings.heading | escape }}</h3>
          <p class="haze-value-props__body">{{ block.settings.body | escape }}</p>
        </div>
      {%- endif -%}
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Haze Value Props",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "blocks": [
    {
      "type": "prop",
      "name": "Value Prop",
      "settings": [
        { "type": "text", "id": "icon", "label": "Icon filename (e.g. haze-icon-ritual.svg)" },
        { "type": "text", "id": "heading", "label": "Heading", "default": "A Complete Ritual" },
        { "type": "text", "id": "body", "label": "Body text", "default": "Every tool your hair needs, curated in one luxurious kit." }
      ]
    }
  ],
  "presets": [{
    "name": "Haze Value Props",
    "blocks": [
      { "type": "prop", "settings": { "icon": "haze-icon-ritual.svg", "heading": "A Complete Ritual", "body": "Every tool your hair needs, curated in one luxurious kit." }},
      { "type": "prop", "settings": { "icon": "haze-icon-leaf.svg", "heading": "Thoughtfully Sourced", "body": "OEKO-TEX certified towel. Silk-grade comb. Materials that care." }},
      { "type": "prop", "settings": { "icon": "haze-icon-gift.svg", "heading": "Gift-Ready Always", "body": "Arrives in a keepsake gift box — no wrapping required." }}
    ]
  }]
}
{% endschema %}
```

Key details:
- Block type is `"prop"` (not `"card"` — avoids collision with product grid's block type)
- Icon rendered as `<img>` from assets, with `aria-hidden="true"` and empty `alt=""` (decorative)
- `{{ block.shopify_attributes }}` for Customize panel highlighting
- Text uses `| escape` filter for safety
- Preset pre-populates all 3 value props with default content from UI-SPEC Copywriting Contract

**Create `assets/haze-value-props.css`** with these exact rules:

```css
/* Value props section — 3 columns desktop, stacked mobile (per D-16) */
.haze-value-props {
  padding: var(--haze-space-xl) var(--haze-space-lg);
  background-color: var(--haze-color-cream);
}

.haze-value-props__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--haze-space-md);
  max-width: 1200px;
  margin: 0 auto;
}

@media screen and (min-width: 750px) {
  .haze-value-props__grid {
    grid-template-columns: repeat(3, 1fr);
    gap: var(--haze-space-lg);
  }
}

.haze-value-props__block {
  text-align: center;
}

/* Icon: 40x40, terracotta color via currentColor */
.haze-value-props__icon {
  margin-bottom: var(--haze-space-sm);
  color: var(--haze-color-terracotta);
}

.haze-value-props__icon img {
  width: 40px;
  height: 40px;
  display: block;
  margin: 0 auto;
}

/* Heading: Heading role — 24px (1.5rem), Cormorant Garamond, weight 400 */
.haze-value-props__heading {
  font-family: var(--font-heading-family);
  font-size: 1.5rem;
  font-weight: 400;
  line-height: 1.2;
  color: var(--haze-color-dark);
  margin: 0 0 var(--haze-space-sm) 0;
}

/* Body: Body role — 16px (1rem), DM Sans, weight 400, opacity 0.8 */
.haze-value-props__body {
  font-family: var(--font-body-family);
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.5;
  color: var(--haze-color-dark);
  opacity: 0.8;
  margin: 0;
}
```

Typography mapping (from UI-SPEC — no deviations):
- Block heading: Heading role — 24px (1.5rem), Cormorant Garamond, weight 400, line-height 1.2
- Block body: Body role — 16px (1rem), DM Sans, weight 400, line-height 1.5, opacity 0.8

Spacing (from UI-SPEC):
- Section padding: `var(--haze-space-xl)` (64px) top/bottom, `var(--haze-space-lg)` (32px) left/right
- Grid gap: `var(--haze-space-md)` (16px mobile), `var(--haze-space-lg)` (32px desktop at 750px+)
- Icon to heading gap: `var(--haze-space-sm)` (8px)
- Heading to body gap: `var(--haze-space-sm)` (8px)

Icon styling:
- Icons are inline SVGs using `currentColor` — the `.haze-value-props__icon` sets `color: var(--haze-color-terracotta)` so the SVG strokes render in terracotta
- Icon images rendered as `<img>` tags with `currentColor` in the SVG will NOT inherit CSS color (since `<img>` doesn't pass CSS context to SVG). Instead, use the SVGs as-is with their `stroke="currentColor"` — the `currentColor` in `<img>` defaults to black. To get terracotta color, either: (a) use CSS `filter` to colorize, or (b) hardcode `stroke="#C4714A"` in the SVG files instead of `currentColor`. **Use option (b)**: set `stroke="#C4714A"` in all 3 SVG files since these are single-use brand icons. The color aliases in haze-tokens.css are for CSS files; SVG fill/stroke must use raw hex since they're not CSS-styled when loaded as `<img>`.
  </action>
  <verify>
    <automated>cd "/Users/ostin/The Vault/电商project/haze-shopify-theme" && test -f sections/haze-value-props.liquid && test -f assets/haze-value-props.css && test -f assets/haze-icon-ritual.svg && test -f assets/haze-icon-leaf.svg && test -f assets/haze-icon-gift.svg && grep -q "haze-value-props.css.*asset_url.*stylesheet_tag" sections/haze-value-props.liquid && grep -q "{% schema %}" sections/haze-value-props.liquid && grep -q "A Complete Ritual" sections/haze-value-props.liquid && grep -q "Thoughtfully Sourced" sections/haze-value-props.liquid && grep -q "Gift-Ready Always" sections/haze-value-props.liquid && grep -q "repeat(3, 1fr)" assets/haze-value-props.css && grep -q "haze-color-terracotta" assets/haze-value-props.css && grep -q "<svg" assets/haze-icon-ritual.svg && grep -q "<svg" assets/haze-icon-leaf.svg && grep -q "<svg" assets/haze-icon-gift.svg && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/haze-value-props.liquid exists and contains `{{ 'haze-value-props.css' | asset_url | stylesheet_tag }}` as first content line
    - sections/haze-value-props.liquid contains `{% schema %}` as the last block
    - sections/haze-value-props.liquid schema defines blocks with `"type": "prop"` and settings for icon, heading, body
    - sections/haze-value-props.liquid preset contains 3 blocks with headings: "A Complete Ritual", "Thoughtfully Sourced", "Gift-Ready Always"
    - sections/haze-value-props.liquid preset block icons reference: haze-icon-ritual.svg, haze-icon-leaf.svg, haze-icon-gift.svg
    - sections/haze-value-props.liquid contains `{{ block.shopify_attributes }}` on each block div
    - sections/haze-value-props.liquid contains `| escape` on heading and body output
    - assets/haze-value-props.css contains `.haze-value-props__grid` with `grid-template-columns: 1fr` (mobile default)
    - assets/haze-value-props.css contains `grid-template-columns: repeat(3, 1fr)` in 750px media query
    - assets/haze-value-props.css contains `.haze-value-props__heading` with `font-size: 1.5rem` (Heading role)
    - assets/haze-value-props.css contains `.haze-value-props__body` with `opacity: 0.8` (Body role)
    - assets/haze-value-props.css uses only `var(--haze-*)` tokens for colors and spacing — no hardcoded hex
    - assets/haze-icon-ritual.svg exists, contains `<svg` with `width="40" height="40"`
    - assets/haze-icon-leaf.svg exists, contains `<svg` with `width="40" height="40"`
    - assets/haze-icon-gift.svg exists, contains `<svg` with `width="40" height="40"`
    - All 3 SVG files use `stroke="#C4714A"` (terracotta hex) for brand-consistent icon color
  </acceptance_criteria>
  <done>
    Value props section renders 3 blocks (A Complete Ritual, Thoughtfully Sourced, Gift-Ready Always) in a 3-column grid on desktop (750px+) and stacked on mobile. Each block has a 40x40 terracotta SVG icon, heading in Cormorant Garamond (1.5rem), and body text in DM Sans (1rem, 0.8 opacity). Section is schema-configurable via Customize panel.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create social proof and bottom CTA sections</name>
  <files>sections/haze-social-proof.liquid, assets/haze-social-proof.css, sections/haze-bottom-cta.liquid, assets/haze-bottom-cta.css</files>
  <read_first>
    - assets/haze-tokens.css (design tokens — spacing, colors, radius, fonts)
    - .planning/phases/02-core-pages/02-UI-SPEC.md (social proof component spec, bottom CTA component spec, typography, copywriting contract)
    - .planning/phases/02-core-pages/02-RESEARCH.md (Pattern 6: conditional section rendering, star rating SVG code example, Pattern 1 variant for bottom CTA)
  </read_first>
  <action>
**Create `sections/haze-social-proof.liquid`** — hidden testimonials section (per D-18, D-19, D-20).

Full section structure:
```liquid
{%- if section.settings.show_section -%}
  {{ 'haze-social-proof.css' | asset_url | stylesheet_tag }}

  <section id="haze-social-proof-{{ section.id }}" class="haze-social-proof">
    {%- if section.settings.heading != blank -%}
      <h2 class="haze-social-proof__heading">{{ section.settings.heading | escape }}</h2>
    {%- endif -%}
    <div class="haze-social-proof__grid">
      {%- for block in section.blocks -%}
        {%- if block.type == 'testimonial' -%}
          <div class="haze-social-proof__card" {{ block.shopify_attributes }}>
            <span class="haze-social-proof__stars" aria-label="5 out of 5 stars" role="img">
              {%- for i in (1..5) -%}
                <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
                  <path d="M8 1.2l1.8 3.7 4 .6-2.9 2.8.7 4-3.6-1.9-3.6 1.9.7-4L2.2 5.5l4-.6L8 1.2z"/>
                </svg>
              {%- endfor -%}
            </span>
            <blockquote class="haze-social-proof__quote">
              &ldquo;{{ block.settings.quote | escape }}&rdquo;
            </blockquote>
            <cite class="haze-social-proof__name">{{ block.settings.name | escape }}</cite>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </section>
{%- endif -%}

{% schema %}
{
  "name": "Haze Social Proof",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    {
      "type": "checkbox",
      "id": "show_section",
      "label": "Show social proof section",
      "default": false,
      "info": "Enable when real reviews are available"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading",
      "default": "What they're saying"
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        { "type": "text", "id": "quote", "label": "Quote" },
        { "type": "text", "id": "name", "label": "Reviewer name" }
      ]
    }
  ],
  "presets": [{
    "name": "Haze Social Proof",
    "settings": { "show_section": false },
    "blocks": [
      { "type": "testimonial", "settings": { "quote": "Finally a routine I actually do every single morning.", "name": "SARAH K." }},
      { "type": "testimonial", "settings": { "quote": "The silk comb alone is worth it. My hair is so much smoother.", "name": "MAYA R." }},
      { "type": "testimonial", "settings": { "quote": "I bought it as a gift and ended up ordering one for myself too.", "name": "JESSICA T." }}
    ]
  }]
}
{% endschema %}
```

Key details:
- The `{% if section.settings.show_section %}` guard wraps ALL content including the `stylesheet_tag` (per RESEARCH.md Pitfall 3 — cleaner approach, no CSS loaded when hidden)
- When `show_section` is false (default), section outputs zero HTML — no DOM nodes at all (per D-20)
- Star rating uses inline SVG with `fill="currentColor"` — CSS sets the color to terracotta
- `<blockquote>` for semantic quote markup, `<cite>` for attribution
- Typographic quotes `&ldquo;` / `&rdquo;` around the quote text (per UI-SPEC)
- Block type is `"testimonial"` (unique, no collision)
- `{{ block.shopify_attributes }}` for Customize panel highlighting
- Preset has `"show_section": false` to match the default hidden state

**Create `assets/haze-social-proof.css`**:

```css
/* Social proof section — 3-column testimonial cards (per D-18/19/20) */
.haze-social-proof {
  padding: var(--haze-space-xl) var(--haze-space-lg);
  background-color: var(--haze-color-cream);
}

.haze-social-proof__heading {
  font-family: var(--font-heading-family);
  font-size: 1.5rem;
  font-weight: 400;
  line-height: 1.2;
  color: var(--haze-color-dark);
  text-align: center;
  margin: 0 0 var(--haze-space-lg) 0;
}

.haze-social-proof__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--haze-space-md);
  max-width: 1200px;
  margin: 0 auto;
}

@media screen and (min-width: 750px) {
  .haze-social-proof__grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* Testimonial card */
.haze-social-proof__card {
  background-color: var(--haze-color-blush);
  border-radius: var(--haze-radius-card);
  padding: var(--haze-space-md);
}

/* Star rating — terracotta via currentColor */
.haze-social-proof__stars {
  display: flex;
  gap: 2px;
  margin-bottom: var(--haze-space-sm);
  color: var(--haze-color-terracotta);
}

.haze-social-proof__stars svg {
  display: block;
}

/* Quote: Body role — 16px, italic, Cormorant Garamond */
.haze-social-proof__quote {
  font-family: var(--font-heading-family);
  font-size: 1rem;
  font-weight: 400;
  font-style: italic;
  line-height: 1.5;
  color: var(--haze-color-dark);
  margin: 0 0 var(--haze-space-sm) 0;
  border: none;
  padding: 0;
}

/* Reviewer name: Label role — 13px, uppercase, letter-spacing */
.haze-social-proof__name {
  font-family: var(--font-body-family);
  font-size: 0.8125rem;
  font-weight: 400;
  font-style: normal;
  line-height: 1.3;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--haze-color-dark);
  opacity: 0.6;
}
```

Typography mapping (from UI-SPEC — no deviations):
- Section heading: Heading role — 24px (1.5rem), Cormorant Garamond, weight 400, line-height 1.2
- Testimonial quote: Body role — 16px (1rem), Cormorant Garamond (italic variant per UI-SPEC: "italic"), weight 400, line-height 1.5
- Reviewer name: Label role — 13px (0.8125rem), DM Sans, weight 400, uppercase, letter-spacing 0.08em, opacity 0.6

---

**Create `sections/haze-bottom-cta.liquid`** — compact CTA section (per D-21, D-22).

Full section structure:
```liquid
{{ 'haze-bottom-cta.css' | asset_url | stylesheet_tag }}

<section id="haze-bottom-cta-{{ section.id }}" class="haze-bottom-cta">
  <div class="haze-bottom-cta__content">
    <h2 class="haze-bottom-cta__headline">{{ section.settings.headline | escape }}</h2>
    {%- if section.settings.subtext != blank -%}
      <p class="haze-bottom-cta__subtext">{{ section.settings.subtext | escape }}</p>
    {%- endif -%}
    <a href="{{ section.settings.cta_link }}" class="haze-bottom-cta__cta">
      {{- section.settings.cta_label | escape -}}
    </a>
  </div>
</section>

{% schema %}
{
  "name": "Haze Bottom CTA",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    { "type": "text", "id": "headline", "label": "Headline", "default": "Your hair ritual starts here." },
    { "type": "text", "id": "subtext", "label": "Subtext", "default": "The Hair Ritual Kit — $59, free shipping." },
    { "type": "text", "id": "cta_label", "label": "CTA button label", "default": "Shop the Kit" },
    { "type": "url", "id": "cta_link", "label": "CTA button link", "default": "/products/hair-ritual-kit" }
  ],
  "presets": [{ "name": "Haze Bottom CTA" }]
}
{% endschema %}
```

Key details:
- Uses blush background via CSS (not Dawn color scheme class — simpler for this self-contained section)
- `type: url` for CTA link (Shopify validates URLs)
- Subtext is optional (conditional render)
- Schema has defaults matching UI-SPEC Copywriting Contract

**Create `assets/haze-bottom-cta.css`**:

```css
/* Bottom CTA — compact hero variant, blush background (per D-21/22) */
.haze-bottom-cta {
  padding: var(--haze-space-lg) var(--haze-space-md);
  background-color: var(--haze-color-blush);
}

.haze-bottom-cta__content {
  text-align: center;
  max-width: 600px;
  margin: 0 auto;
}

/* Headline: Heading role — 24px (1.5rem), Cormorant Garamond, weight 400 */
.haze-bottom-cta__headline {
  font-family: var(--font-heading-family);
  font-size: 1.5rem;
  font-weight: 400;
  line-height: 1.2;
  color: var(--haze-color-dark);
  margin: 0 0 var(--haze-space-sm) 0;
}

/* Subtext: Body role — 16px (1rem), DM Sans, weight 400, opacity 0.75 */
.haze-bottom-cta__subtext {
  font-family: var(--font-body-family);
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.5;
  color: var(--haze-color-dark);
  opacity: 0.75;
  margin: 0 0 var(--haze-space-md) 0;
}

/* CTA button: same style as hero CTA — terracotta fill, cream text */
.haze-bottom-cta__cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 12px 32px;
  background-color: var(--haze-color-terracotta);
  color: var(--haze-color-cream);
  border-radius: var(--haze-radius-button);
  font-family: var(--font-body-family);
  font-size: 0.8125rem;
  font-weight: 400;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  text-decoration: none;
  border: none;
  cursor: pointer;
  transition: opacity 0.2s ease;
}

.haze-bottom-cta__cta:hover,
.haze-bottom-cta__cta:focus-visible {
  opacity: 0.85;
}
```

Typography mapping (from UI-SPEC — no deviations):
- Headline: Heading role — 24px (1.5rem), Cormorant Garamond, weight 400, line-height 1.2
- Subtext: Body role — 16px (1rem), DM Sans, weight 400, line-height 1.5, opacity 0.75
- CTA button: Label role — 13px (0.8125rem), DM Sans, weight 400, letter-spacing 0.12em, uppercase

Spacing (from UI-SPEC):
- Section padding: `var(--haze-space-lg)` (32px) top/bottom — intentionally smaller than hero (per D-22)
- Side padding: `var(--haze-space-md)` (16px) left/right
- CTA min-height: 44px (WCAG 2.5.5 target size)

Color (from UI-SPEC):
- Section background: `var(--haze-color-blush)` (color-scheme-2 equivalent)
- Text: `var(--haze-color-dark)`
- CTA: terracotta fill + cream text (same as hero CTA — per UI-SPEC consistency)
  </action>
  <verify>
    <automated>cd "/Users/ostin/The Vault/电商project/haze-shopify-theme" && test -f sections/haze-social-proof.liquid && test -f assets/haze-social-proof.css && test -f sections/haze-bottom-cta.liquid && test -f assets/haze-bottom-cta.css && grep -q "show_section" sections/haze-social-proof.liquid && grep -q "SARAH K." sections/haze-social-proof.liquid && grep -q "MAYA R." sections/haze-social-proof.liquid && grep -q "JESSICA T." sections/haze-social-proof.liquid && grep -q "default.*false" sections/haze-social-proof.liquid && grep -q "haze-color-blush" assets/haze-bottom-cta.css && grep -q "haze-color-terracotta" assets/haze-bottom-cta.css && grep -q "Your hair ritual starts here" sections/haze-bottom-cta.liquid && grep -q "Shop the Kit" sections/haze-bottom-cta.liquid && grep -q "min-height: 44px" assets/haze-bottom-cta.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/haze-social-proof.liquid exists with `{%- if section.settings.show_section -%}` guard wrapping ALL content
    - sections/haze-social-proof.liquid stylesheet_tag is INSIDE the if guard (not outside)
    - sections/haze-social-proof.liquid schema has `"show_section"` checkbox setting with `"default": false`
    - sections/haze-social-proof.liquid schema defines `"testimonial"` block type with quote and name settings
    - sections/haze-social-proof.liquid preset contains 3 testimonials: SARAH K., MAYA R., JESSICA T. with exact quotes from UI-SPEC
    - sections/haze-social-proof.liquid contains inline SVG star rating (5 stars, `fill="currentColor"`, 16x16)
    - sections/haze-social-proof.liquid contains `aria-label="5 out of 5 stars"` on stars span
    - assets/haze-social-proof.css contains `.haze-social-proof__card` with `background-color: var(--haze-color-blush)`
    - assets/haze-social-proof.css contains `.haze-social-proof__stars` with `color: var(--haze-color-terracotta)`
    - assets/haze-social-proof.css contains `.haze-social-proof__quote` with `font-style: italic`
    - assets/haze-social-proof.css contains `.haze-social-proof__name` with `letter-spacing: 0.08em` and `text-transform: uppercase` and `opacity: 0.6`
    - assets/haze-social-proof.css contains `grid-template-columns: repeat(3, 1fr)` in 750px media query
    - sections/haze-bottom-cta.liquid exists with `{{ 'haze-bottom-cta.css' | asset_url | stylesheet_tag }}` as first content line
    - sections/haze-bottom-cta.liquid schema has defaults: headline "Your hair ritual starts here.", subtext "The Hair Ritual Kit — $59, free shipping.", cta_label "Shop the Kit"
    - sections/haze-bottom-cta.liquid schema has `"presets": [{ "name": "Haze Bottom CTA" }]`
    - assets/haze-bottom-cta.css contains `.haze-bottom-cta` with `background-color: var(--haze-color-blush)`
    - assets/haze-bottom-cta.css contains `.haze-bottom-cta` with `padding: var(--haze-space-lg) var(--haze-space-md)` (compact, not xl)
    - assets/haze-bottom-cta.css contains `.haze-bottom-cta__cta` with `min-height: 44px` and `background-color: var(--haze-color-terracotta)`
    - assets/haze-bottom-cta.css contains `.haze-bottom-cta__cta` with `letter-spacing: 0.12em` and `text-transform: uppercase`
    - No hardcoded hex values in any CSS file — all colors via `var(--haze-color-*)` tokens
  </acceptance_criteria>
  <done>
    Social proof section outputs zero HTML when show_section is false (default). When enabled, it renders 3 testimonial cards with 5-star terracotta ratings, italic quotes, and uppercase reviewer names on blush background cards. Bottom CTA section renders with blush background, "Your hair ritual starts here." headline, subtext, and terracotta "Shop the Kit" button linking to PDP. Both sections are schema-configurable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire all sections into homepage template</name>
  <files>templates/index.json</files>
  <read_first>
    - templates/index.json (current state — Dawn default sections to be fully replaced)
    - sections/haze-hero.liquid (verify section exists and schema structure)
    - sections/haze-product-grid.liquid (verify section exists and schema/block structure)
    - sections/haze-value-props.liquid (verify section exists and schema/block structure)
    - sections/haze-social-proof.liquid (verify section exists and schema structure)
    - sections/haze-bottom-cta.liquid (verify section exists and schema structure)
    - .planning/phases/02-core-pages/02-RESEARCH.md (Pattern 8: index.json replacement — exact target JSON)
  </read_first>
  <action>
**Replace `templates/index.json`** with the Haze homepage template (per D-23).

The entire file content must be replaced with this exact JSON:

```json
{
  "sections": {
    "haze_hero": {
      "type": "haze-hero",
      "settings": {
        "headline": "Stay tender.",
        "subtext": "A complete hair ritual, beautifully made.",
        "cta_label": "Shop the Kit",
        "cta_link": "/products/hair-ritual-kit"
      }
    },
    "haze_product_grid": {
      "type": "haze-product-grid",
      "blocks": {
        "card_silk_comb": {
          "type": "card",
          "settings": {
            "name": "Silk Comb",
            "placeholder_image": "haze-placeholder-silk-comb.jpg"
          }
        },
        "card_brush": {
          "type": "card",
          "settings": {
            "name": "Boar-Bristle Brush",
            "placeholder_image": "haze-placeholder-brush.jpg"
          }
        },
        "card_towel": {
          "type": "card",
          "settings": {
            "name": "OEKO-TEX Towel",
            "placeholder_image": "haze-placeholder-towel.jpg"
          }
        },
        "card_scrunchie": {
          "type": "card",
          "settings": {
            "name": "Silk Scrunchie",
            "placeholder_image": "haze-placeholder-scrunchie.jpg"
          }
        },
        "card_gift_box": {
          "type": "card",
          "settings": {
            "name": "Gift Box",
            "placeholder_image": "haze-placeholder-gift-box.jpg"
          }
        }
      },
      "block_order": [
        "card_silk_comb",
        "card_brush",
        "card_towel",
        "card_scrunchie",
        "card_gift_box"
      ]
    },
    "haze_value_props": {
      "type": "haze-value-props",
      "blocks": {
        "prop_ritual": {
          "type": "prop",
          "settings": {
            "icon": "haze-icon-ritual.svg",
            "heading": "A Complete Ritual",
            "body": "Every tool your hair needs, curated in one luxurious kit."
          }
        },
        "prop_sourced": {
          "type": "prop",
          "settings": {
            "icon": "haze-icon-leaf.svg",
            "heading": "Thoughtfully Sourced",
            "body": "OEKO-TEX certified towel. Silk-grade comb. Materials that care."
          }
        },
        "prop_gift": {
          "type": "prop",
          "settings": {
            "icon": "haze-icon-gift.svg",
            "heading": "Gift-Ready Always",
            "body": "Arrives in a keepsake gift box \u2014 no wrapping required."
          }
        }
      },
      "block_order": [
        "prop_ritual",
        "prop_sourced",
        "prop_gift"
      ]
    },
    "haze_social_proof": {
      "type": "haze-social-proof",
      "settings": {
        "show_section": false,
        "heading": "What they're saying"
      },
      "blocks": {
        "testimonial_1": {
          "type": "testimonial",
          "settings": {
            "quote": "Finally a routine I actually do every single morning.",
            "name": "SARAH K."
          }
        },
        "testimonial_2": {
          "type": "testimonial",
          "settings": {
            "quote": "The silk comb alone is worth it. My hair is so much smoother.",
            "name": "MAYA R."
          }
        },
        "testimonial_3": {
          "type": "testimonial",
          "settings": {
            "quote": "I bought it as a gift and ended up ordering one for myself too.",
            "name": "JESSICA T."
          }
        }
      },
      "block_order": [
        "testimonial_1",
        "testimonial_2",
        "testimonial_3"
      ]
    },
    "haze_bottom_cta": {
      "type": "haze-bottom-cta",
      "settings": {
        "headline": "Your hair ritual starts here.",
        "subtext": "The Hair Ritual Kit \u2014 $59, free shipping.",
        "cta_label": "Shop the Kit",
        "cta_link": "/products/hair-ritual-kit"
      }
    }
  },
  "order": [
    "haze_hero",
    "haze_product_grid",
    "haze_value_props",
    "haze_social_proof",
    "haze_bottom_cta"
  ]
}
```

Key details:
- Section order per D-23: hero -> product grid -> value props -> social proof -> bottom CTA
- Section keys use underscores (Shopify convention for JSON keys): `haze_hero`, `haze_product_grid`, etc.
- Section `type` values use hyphens (matching section filenames): `haze-hero`, `haze-product-grid`, etc.
- Product grid blocks pre-populate all 5 cards with correct names and placeholder image filenames (per D-09)
- Value props blocks pre-populate all 3 props with icons, headings, and body text
- Social proof settings has `"show_section": false` (hidden by default per D-18/D-20)
- Social proof blocks pre-populate 3 testimonials matching UI-SPEC Copywriting Contract
- Both CTA sections (hero and bottom) link to `/products/hair-ritual-kit`
- Em dashes in text use `\u2014` (JSON Unicode escape for —)
- All Dawn default sections (image_banner, rich_text, featured_collection, collage, video, multicolumn) are completely removed

**CRITICAL ordering:** This task MUST run AFTER Tasks 1 and 2 (which create the section files). If `templates/index.json` references a section type that doesn't exist as a file, Shopify will reject the theme push. All 5 section files must exist before this file is written.
  </action>
  <verify>
    <automated>cd "/Users/ostin/The Vault/电商project/haze-shopify-theme" && grep -q "haze-hero" templates/index.json && grep -q "haze-product-grid" templates/index.json && grep -q "haze-value-props" templates/index.json && grep -q "haze-social-proof" templates/index.json && grep -q "haze-bottom-cta" templates/index.json && grep -q '"show_section": false' templates/index.json && python3 -c "import json; f=open('templates/index.json'); d=json.load(f); assert d['order']==['haze_hero','haze_product_grid','haze_value_props','haze_social_proof','haze_bottom_cta'], 'wrong order'; assert len(d['sections'])==5, 'wrong section count'; print('PASS')" && echo "JSON valid" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - templates/index.json is valid JSON (parseable by python3 json.load)
    - templates/index.json `"order"` array is exactly: ["haze_hero", "haze_product_grid", "haze_value_props", "haze_social_proof", "haze_bottom_cta"]
    - templates/index.json contains exactly 5 section entries (no Dawn default sections remain)
    - templates/index.json `haze_hero` section has settings with headline "Stay tender.", cta_link "/products/hair-ritual-kit"
    - templates/index.json `haze_product_grid` section has 5 blocks with correct card names: Silk Comb, Boar-Bristle Brush, OEKO-TEX Towel, Silk Scrunchie, Gift Box
    - templates/index.json `haze_product_grid` block_order has 5 entries in correct order
    - templates/index.json `haze_value_props` section has 3 blocks with correct prop headings and icon filenames
    - templates/index.json `haze_social_proof` section has `"show_section": false`
    - templates/index.json `haze_social_proof` section has 3 testimonial blocks with correct names (SARAH K., MAYA R., JESSICA T.)
    - templates/index.json `haze_bottom_cta` section has settings with headline "Your hair ritual starts here." and cta_link "/products/hair-ritual-kit"
    - templates/index.json does NOT contain "image_banner", "rich_text", "featured_collection", "collage", or "multicolumn" (Dawn defaults fully removed)
  </acceptance_criteria>
  <done>
    Homepage template wires all 5 Haze sections in correct order with all settings, blocks, and block_order pre-populated. Dawn's default sections are fully replaced. Social proof section is hidden by default. Both CTA buttons link to the Hair Ritual Kit PDP.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema settings to HTML | User-editable text (headings, body, quotes, names) rendered into HTML via Liquid |
| Icon filenames to asset_url | Text settings used in `asset_url` filter to construct CDN paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Injection | haze-value-props.liquid | accept | All text output uses `{{ ... \| escape }}` filter; Shopify schema `type: text` is plain text only |
| T-02-05 | Injection | haze-social-proof.liquid | accept | Quote and name output uses `{{ ... \| escape }}` filter; section settings are admin-only |
| T-02-06 | Injection | haze-bottom-cta.liquid | accept | Text settings use `{{ ... \| escape }}`; URL setting uses `type: url` (Shopify-validated) |
| T-02-07 | Info Disclosure | haze-social-proof.liquid | accept | Placeholder testimonial names (SARAH K., MAYA R., JESSICA T.) are fictional; no real PII. Will be replaced with real reviews in V2-04 |
</threat_model>

<verification>
1. `sections/haze-value-props.liquid` renders 3 blocks with icons, headings, and body text in a 3-column grid
2. `assets/haze-value-props.css` implements responsive grid (1 col mobile, 3 col at 750px+)
3. 3 SVG icon files exist in `assets/` with correct dimensions (40x40) and terracotta color
4. `sections/haze-social-proof.liquid` outputs zero HTML when `show_section` is false
5. `sections/haze-social-proof.liquid` renders 3 testimonial cards when enabled
6. `sections/haze-bottom-cta.liquid` uses blush background and terracotta CTA
7. `templates/index.json` lists all 5 sections in correct order with all settings pre-populated
8. No Dawn default sections remain in `templates/index.json`
9. All CSS files use design token custom properties — no hardcoded hex
</verification>

<success_criteria>
- All 3 new sections and their CSS files exist and are syntactically correct
- Value props grid responds correctly at 750px breakpoint (1 col -> 3 col)
- Social proof section is hidden by default (zero HTML output)
- Bottom CTA uses blush background with compact padding (32px, not 64px)
- Homepage template references all 5 Haze sections in order
- All CTA buttons link to `/products/hair-ritual-kit`
- No hardcoded hex in CSS (all via `var(--haze-color-*)` tokens)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-03-SUMMARY.md`
</output>
