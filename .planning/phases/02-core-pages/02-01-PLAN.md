---
phase: 02-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/haze-hero.liquid
  - assets/haze-hero.css
autonomous: true
requirements:
  - HOME-01

must_haves:
  truths:
    - "The homepage hero occupies the full viewport height with a placeholder background image"
    - "The 'Stay tender.' headline renders in Cormorant Garamond italic at 56px desktop / 36px mobile"
    - "The 'Shop the Kit' CTA button navigates to /products/hair-ritual-kit"
    - "The CTA uses terracotta fill, cream text, 2px radius, and is at least 44px tall"
  artifacts:
    - path: "sections/haze-hero.liquid"
      provides: "Full-viewport hero section with schema"
      contains: "{% schema %}"
    - path: "assets/haze-hero.css"
      provides: "Hero styling with viewport height, flexbox centering, CTA button"
      contains: ".haze-hero"
  key_links:
    - from: "sections/haze-hero.liquid"
      to: "assets/haze-hero.css"
      via: "stylesheet_tag"
      pattern: "haze-hero.css.*asset_url.*stylesheet_tag"
---

<objective>
Create the full-viewport hero section for the Haze homepage.

Purpose: The hero is the first thing TikTok visitors see when they land on haze.hair. It must immediately communicate the brand aesthetic ("Stay tender." headline, editorial warm feel) and provide a single clear CTA to the product page.

Output: `sections/haze-hero.liquid` with its own CSS file `assets/haze-hero.css`, ready to be referenced by `templates/index.json` in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages/02-CONTEXT.md
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-UI-SPEC.md
@assets/haze-tokens.css

<interfaces>
<!-- Design tokens available from haze-tokens.css (loaded globally in theme.liquid) -->
:root {
  --haze-color-terracotta: #C4714A;
  --haze-color-cream:      #F7F3EE;
  --haze-color-blush:      #E8D5CE;
  --haze-color-dark:       #2C2420;
  --haze-space-xs:  0.4rem;
  --haze-space-sm:  0.8rem;
  --haze-space-md:  1.6rem;
  --haze-space-lg:  3.2rem;
  --haze-space-xl:  6.4rem;
  --haze-radius-button: 2px;
  --haze-radius-card:   4px;
  --font-heading-family: 'Cormorant Garamond', Georgia, serif !important;
  --font-body-family:    'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif !important;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero section (Liquid + CSS)</name>
  <files>sections/haze-hero.liquid, assets/haze-hero.css</files>
  <read_first>
    - assets/haze-tokens.css (design tokens — spacing, colors, radius, fonts)
    - sections/image-banner.liquid (reference for Dawn section schema conventions — DO NOT reuse per D-01, study only)
    - .planning/phases/02-core-pages/02-UI-SPEC.md (typography assignments, spacing, color usage)
    - .planning/phases/02-core-pages/02-RESEARCH.md (Pattern 1: hero section structure, hero CSS code example)
  </read_first>
  <action>
**Create `sections/haze-hero.liquid`** — a custom full-viewport hero section (per D-01, do NOT reuse Dawn's image-banner.liquid).

The Liquid file must contain these elements in order:

1. **CSS loading** (first line): `{{ 'haze-hero.css' | asset_url | stylesheet_tag }}` (per D-24)

2. **Section HTML structure:**
   ```
   <section id="haze-hero-{{ section.id }}" class="haze-hero">
     <div class="haze-hero__media">
       {%- comment -%} PLACEHOLDER — replace with real hero photography {%- endcomment -%}
       <img src="{{ 'haze-placeholder-hero.jpg' | asset_url }}" alt="Haze Hair Ritual Kit — placeholder" class="haze-hero__image" loading="eager" width="1920" height="1080" fetchpriority="high">
     </div>
     <div class="haze-hero__scrim"></div>
     <div class="haze-hero__content">
       <h1 class="haze-hero__headline">{{ section.settings.headline }}</h1>
       <p class="haze-hero__subtext">{{ section.settings.subtext }}</p>
       <a href="{{ section.settings.cta_link }}" class="haze-hero__cta">{{ section.settings.cta_label }}</a>
     </div>
   </section>
   ```

   Key details:
   - Hero image uses `loading="eager"` and `fetchpriority="high"` (above the fold, first paint)
   - `{%- comment -%} PLACEHOLDER` tag for grep-findability (per D-03)
   - `haze-hero__scrim` div creates dark overlay for text legibility (rgba(44, 36, 32, 0.25))
   - Content centered via flexbox on the parent `.haze-hero`
   - Headline, subtext, and CTA all read from section schema settings

3. **Schema block** (must be LAST in the file):
   ```json
   {
     "name": "Haze Hero",
     "tag": "section",
     "class": "section",
     "disabled_on": { "groups": ["header", "footer"] },
     "settings": [
       { "type": "text", "id": "headline", "label": "Headline", "default": "Stay tender." },
       { "type": "text", "id": "subtext", "label": "Subtext", "default": "A complete hair ritual, beautifully made." },
       { "type": "text", "id": "cta_label", "label": "CTA button label", "default": "Shop the Kit" },
       { "type": "url", "id": "cta_link", "label": "CTA button link", "default": "/products/hair-ritual-kit" }
     ],
     "presets": [{ "name": "Haze Hero" }]
   }
   ```

**Create `assets/haze-hero.css`** with these exact rules:

```css
.haze-hero {
  position: relative;
  height: 100vh;
  height: 100dvh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.haze-hero__media {
  position: absolute;
  inset: 0;
}

.haze-hero__image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.haze-hero__scrim {
  position: absolute;
  inset: 0;
  background: rgba(44, 36, 32, 0.25);
}

.haze-hero__content {
  position: relative;
  z-index: 1;
  text-align: center;
  max-width: 600px;
  padding: 0 var(--haze-space-md);
  color: var(--haze-color-cream);
}

.haze-hero__headline {
  font-family: var(--font-heading-family);
  font-size: 36px;
  font-weight: 300;
  font-style: italic;
  line-height: 1.1;
  margin: 0 0 var(--haze-space-sm) 0;
}

@media screen and (min-width: 750px) {
  .haze-hero__headline {
    font-size: 56px;
  }
}

.haze-hero__subtext {
  font-family: var(--font-body-family);
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.5;
  opacity: 0.8;
  margin: 0 0 var(--haze-space-md) 0;
}

.haze-hero__cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  padding: 12px 32px;
  background-color: var(--haze-color-terracotta);
  color: var(--haze-color-cream);
  border-radius: var(--haze-radius-button);
  font-family: var(--font-body-family);
  font-size: 0.8125rem;
  font-weight: 400;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  text-decoration: none;
  border: none;
  cursor: pointer;
  transition: opacity 0.2s ease;
}

.haze-hero__cta:hover,
.haze-hero__cta:focus-visible {
  opacity: 0.85;
}
```

Typography mapping (from UI-SPEC Type Scale — no deviations allowed):
- Headline: Display role — 56px desktop / 36px mobile, Cormorant Garamond, weight 300, italic, line-height 1.1
- Subtext: Body role — 16px (1rem), DM Sans, weight 400, line-height 1.5, opacity 0.8
- CTA button: Label role — 13px (0.8125rem), DM Sans, weight 400, letter-spacing 0.12em, uppercase

Color usage (per UI-SPEC Color section):
- Hero background: cream behind the image (will show during load)
- Content text: `--haze-color-cream` (white/cream on dark scrim)
- CTA fill: `--haze-color-terracotta` (accent, per D-04)
- CTA text: `--haze-color-cream`
- Scrim: `rgba(44, 36, 32, 0.25)` — dark at 25% opacity for text legibility

No hex values in CSS except the scrim rgba (which uses the dark color with opacity and cannot be expressed as a CSS custom property with alpha channel).
  </action>
  <verify>
    <automated>cd "/Users/ostin/The Vault/电商project/haze-shopify-theme" && test -f sections/haze-hero.liquid && test -f assets/haze-hero.css && grep -q "haze-hero.css.*asset_url.*stylesheet_tag" sections/haze-hero.liquid && grep -q "PLACEHOLDER" sections/haze-hero.liquid && grep -q "{% schema %}" sections/haze-hero.liquid && grep -q "100dvh" assets/haze-hero.css && grep -q "haze-color-terracotta" assets/haze-hero.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/haze-hero.liquid exists and contains `{{ 'haze-hero.css' | asset_url | stylesheet_tag }}` as first content line
    - sections/haze-hero.liquid contains `{%- comment -%} PLACEHOLDER` tag
    - sections/haze-hero.liquid contains `{% schema %}` as the last block
    - sections/haze-hero.liquid schema has settings: headline (default "Stay tender."), subtext, cta_label (default "Shop the Kit"), cta_link
    - sections/haze-hero.liquid schema has `"presets": [{ "name": "Haze Hero" }]`
    - sections/haze-hero.liquid contains `href="{{ section.settings.cta_link }}"` (CTA link)
    - assets/haze-hero.css contains `.haze-hero` with `height: 100dvh`
    - assets/haze-hero.css contains `.haze-hero__cta` with `background-color: var(--haze-color-terracotta)`
    - assets/haze-hero.css contains `.haze-hero__headline` with `font-size: 36px` (mobile) and `font-size: 56px` (in 750px media query)
    - assets/haze-hero.css contains `.haze-hero__cta` with `min-height: 44px`
    - assets/haze-hero.css contains `letter-spacing: 0.12em` and `text-transform: uppercase` on CTA
    - No hardcoded hex values in CSS except `rgba(44, 36, 32, 0.25)` for the scrim
  </acceptance_criteria>
  <done>
    Hero section renders full-viewport with centered "Stay tender." headline (italic 300 weight, 36px mobile / 56px desktop), subtext at 0.8 opacity, and terracotta CTA button linking to /products/hair-ritual-kit. Dark scrim overlay ensures text legibility. Schema allows editing headline, subtext, CTA label, and CTA link via Customize panel.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema settings to HTML | User-editable text (headline, subtext, cta_label) rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | haze-hero.liquid | accept | Shopify Customize panel sanitizes input server-side; schema `type: text` is plain text only; `type: url` is validated by Shopify. XSS via section settings is not possible in Shopify's architecture |
</threat_model>

<verification>
1. `sections/haze-hero.liquid` exists with correct schema and structure
2. `assets/haze-hero.css` exists with full-viewport, flexbox centering, and CTA styling
3. Hero headline uses Display typography role (56px desktop / 36px mobile, italic, weight 300)
4. CTA uses Label typography role (13px, uppercase, letter-spacing 0.12em)
5. No hex values hardcoded in CSS (except scrim rgba)
6. PLACEHOLDER comment tag present for grep-findability
</verification>

<success_criteria>
- Hero section file passes `shopify theme check` without errors
- CSS uses only design token custom properties (no raw hex except rgba scrim)
- Schema settings allow full content customization via Shopify Customize panel
- Section is ready to be referenced by `templates/index.json` in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-01-SUMMARY.md`
</output>
