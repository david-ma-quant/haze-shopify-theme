---
phase: 03-pdp-supporting-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/haze-ritual-description.liquid
  - assets/haze-ritual-description.css
  - sections/haze-ingredients.liquid
  - assets/haze-ingredients.css
  - assets/haze-icon-silk.svg
  - assets/haze-icon-bristle.svg
  - assets/haze-icon-oeko.svg
  - assets/haze-icon-mulberry.svg
autonomous: true
requirements:
  - PDP-03
  - PDP-04

must_haves:
  truths:
    - "The ritual description section renders a centered editorial block with Cormorant Garamond italic headline and DM Sans body copy"
    - "Four ingredient icon blocks display in a responsive grid (4-col desktop, 2-col tablet/mobile) with terracotta SVG icons"
    - "All text is editable via Shopify Customize panel schema settings"
    - "Section CSS uses only haze-tokens.css custom properties — no hardcoded hex except SVG stroke"
  artifacts:
    - path: "sections/haze-ritual-description.liquid"
      provides: "Ritual description editorial section with schema"
      contains: "{% schema %}"
    - path: "assets/haze-ritual-description.css"
      provides: "Ritual description styling — centered column, typography, spacing"
      contains: ".haze-ritual-description"
    - path: "sections/haze-ingredients.liquid"
      provides: "4-block ingredient grid section with schema"
      contains: "{% schema %}"
    - path: "assets/haze-ingredients.css"
      provides: "Ingredient grid layout — responsive columns, icon sizing, typography"
      contains: ".haze-ingredients"
    - path: "assets/haze-icon-silk.svg"
      provides: "Mulberry silk icon (40x40 stroke art)"
      contains: "stroke"
    - path: "assets/haze-icon-bristle.svg"
      provides: "Boar bristle icon (40x40 stroke art)"
      contains: "stroke"
    - path: "assets/haze-icon-oeko.svg"
      provides: "OEKO-TEX cotton icon (40x40 stroke art)"
      contains: "stroke"
    - path: "assets/haze-icon-mulberry.svg"
      provides: "Silk scrunchie icon (40x40 stroke art)"
      contains: "stroke"
  key_links:
    - from: "sections/haze-ritual-description.liquid"
      to: "assets/haze-ritual-description.css"
      via: "stylesheet_tag"
      pattern: "haze-ritual-description.css.*asset_url.*stylesheet_tag"
    - from: "sections/haze-ingredients.liquid"
      to: "assets/haze-ingredients.css"
      via: "stylesheet_tag"
      pattern: "haze-ingredients.css.*asset_url.*stylesheet_tag"
    - from: "sections/haze-ingredients.liquid"
      to: "assets/haze-icon-silk.svg"
      via: "asset_url on block.settings.icon"
      pattern: "block.settings.icon.*asset_url"
---

<objective>
Create the two custom PDP sections — ritual description and ingredients grid — that appear below the main product section on the Hair Ritual Kit page.

Purpose: The ritual description sets the emotional tone ("this is a morning ritual, not just a comb"), while the ingredients grid communicates material quality (silk, boar bristle, OEKO-TEX cotton, mulberry silk). Together they build the aspirational brand story that converts a TikTok visitor into a buyer.

Output: `sections/haze-ritual-description.liquid` + CSS, `sections/haze-ingredients.liquid` + CSS, and 4 ingredient SVG icons — all ready to be referenced by `templates/product.haze.json` in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pdp-supporting-pages/03-CONTEXT.md
@.planning/phases/03-pdp-supporting-pages/03-RESEARCH.md
@.planning/phases/03-pdp-supporting-pages/03-UI-SPEC.md
@assets/haze-tokens.css
@sections/haze-value-props.liquid

<interfaces>
<!-- Design tokens available from haze-tokens.css (loaded globally in theme.liquid) -->
:root {
  --haze-color-terracotta: #C4714A;
  --haze-color-cream:      #F7F3EE;
  --haze-color-blush:      #E8D5CE;
  --haze-color-dark:       #2C2420;
  --haze-space-xs:  0.4rem;
  --haze-space-sm:  0.8rem;
  --haze-space-md:  1.6rem;
  --haze-space-lg:  3.2rem;
  --haze-space-xl:  6.4rem;
  --haze-radius-button: 2px;
  --haze-radius-card:   4px;
  --font-heading-family: 'Cormorant Garamond', Georgia, serif !important;
  --font-body-family:    'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif !important;
}

<!-- Phase 2 value props section pattern (reference for ingredient blocks) -->
<!-- From sections/haze-value-props.liquid -->
<!-- Uses: img src="{{ block.settings.icon | asset_url }}" for icon loading -->
<!-- Uses: block.shopify_attributes on each block div -->
<!-- Uses: | escape on all text output -->
<!-- Uses: 40x40 SVG icons with stroke="#C4714A" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ritual description section (Liquid + CSS)</name>
  <files>sections/haze-ritual-description.liquid, assets/haze-ritual-description.css</files>
  <read_first>
    - assets/haze-tokens.css (design tokens)
    - sections/haze-value-props.liquid (reference for section pattern — CSS loading, schema, escape filter)
    - .planning/phases/03-pdp-supporting-pages/03-UI-SPEC.md (typography, spacing, color for ritual section)
  </read_first>
  <action>
**Create `sections/haze-ritual-description.liquid`** — a custom full-width editorial section for the PDP (per D-11, D-12, D-13).

The section loads its own CSS, then also loads `haze-pdp-gallery.js` via a deferred script tag (the research doc recommends loading the gallery JS from this section since it is the first custom section below main-product and is guaranteed to render on PDP).

The Liquid file must contain these elements in order:

1. **CSS loading** (first line): `{{ 'haze-ritual-description.css' | asset_url | stylesheet_tag }}`

2. **Gallery JS loading** (second line): `<script src="{{ 'haze-pdp-gallery.js' | asset_url }}" defer="defer"></script>` — this loads the dot indicator JS for the gallery above. It must be `defer` so it runs after the page HTML is parsed.

3. **Section HTML structure:**
```liquid
{{ 'haze-ritual-description.css' | asset_url | stylesheet_tag }}
<script src="{{ 'haze-pdp-gallery.js' | asset_url }}" defer="defer"></script>

<section id="haze-ritual-{{ section.id }}" class="haze-ritual-description">
  <div class="haze-ritual-description__inner page-width">
    {%- if section.settings.show_overline -%}
      <span class="haze-ritual-description__overline">{{ section.settings.overline | escape }}</span>
    {%- endif -%}
    <h2 class="haze-ritual-description__heading">{{ section.settings.heading | escape }}</h2>
    {%- if section.settings.quote != blank -%}
      <p class="haze-ritual-description__quote">{{ section.settings.quote | escape }}</p>
    {%- endif -%}
    <div class="haze-ritual-description__body rte">
      {{ section.settings.body }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Haze Ritual Description",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    {
      "type": "checkbox",
      "id": "show_overline",
      "label": "Show overline label",
      "default": true
    },
    {
      "type": "text",
      "id": "overline",
      "label": "Overline text",
      "default": "The Ritual"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "A morning ritual worth doing."
    },
    {
      "type": "text",
      "id": "quote",
      "label": "Pull-quote (italic accent line)",
      "default": "Your best hair days start before the mirror."
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Description",
      "default": "<p>The Hair Ritual Kit is built around one idea: the tools you use every day should feel worth using. Silk that glides. Bristle that nourishes. Cotton that doesn\u2019t roughen. A scrunchie that doesn\u2019t leave a dent. Five minutes in the morning, every morning.</p>"
    }
  ],
  "presets": [{ "name": "Haze Ritual Description" }]
}
{% endschema %}
```

Key details:
- `show_overline` checkbox toggles the "The Ritual" label (per D-13)
- `heading` uses `| escape` (text type, not richtext)
- `quote` is a pull-quote line rendered in Cormorant Garamond italic (per UI-SPEC — differentiated by italic style, not by size — same 24px Heading role)
- `body` is richtext type — do NOT use `| escape` on richtext (Shopify sanitizes richtext server-side and it contains HTML tags like `<p>`)
- The `rte` class on the body wrapper ensures Dawn's richtext typography styles apply
- `page-width` class on the inner div constrains the max-width to Dawn's page width

**Create `assets/haze-ritual-description.css`** with these exact rules:

```css
/* Haze Ritual Description — editorial block below PDP main product */

.haze-ritual-description {
  padding: var(--haze-space-xl) 0;
  background-color: var(--haze-color-cream);
  text-align: center;
}

.haze-ritual-description__inner {
  max-width: 680px;
  margin: 0 auto;
  padding: 0 var(--haze-space-md);
}

@media screen and (min-width: 750px) {
  .haze-ritual-description__inner {
    padding: 0 var(--haze-space-lg);
  }
}

.haze-ritual-description__overline {
  display: block;
  font-family: var(--font-body-family);
  font-size: 0.8125rem;
  font-weight: 400;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--haze-color-terracotta);
  margin-bottom: var(--haze-space-sm);
}

.haze-ritual-description__heading {
  font-family: var(--font-heading-family);
  font-size: 1.5rem;
  font-weight: 400;
  line-height: 1.2;
  color: var(--haze-color-dark);
  margin: 0 0 var(--haze-space-md) 0;
}

.haze-ritual-description__quote {
  font-family: var(--font-heading-family);
  font-size: 1.5rem;
  font-weight: 400;
  font-style: italic;
  line-height: 1.2;
  color: var(--haze-color-dark);
  margin: 0 0 var(--haze-space-md) 0;
}

.haze-ritual-description__body {
  font-family: var(--font-body-family);
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.5;
  color: var(--haze-color-dark);
  opacity: 0.85;
}

.haze-ritual-description__body p {
  margin: 0;
}
```

Typography mapping (from UI-SPEC Per-Component Typography Assignments):
- Overline: Label role — 13px, DM Sans, weight 400, uppercase, letter-spacing 0.12em, terracotta color
- Heading: Heading role — 24px (1.5rem), Cormorant Garamond, weight 400, line-height 1.2
- Pull-quote: Heading role — 24px (1.5rem), Cormorant Garamond, weight 400, italic (same size, differentiated by style)
- Body: Body role — 16px (1rem), DM Sans, weight 400, line-height 1.5, opacity 0.85

Spacing (from UI-SPEC Spacing Scale):
- Section vertical padding: `var(--haze-space-xl)` (64px)
- Inner max-width: 680px (editorial column feel per UI-SPEC)
- Overline → heading gap: `var(--haze-space-sm)` (8px)
- Heading → body gap: `var(--haze-space-md)` (16px)
- Horizontal padding: `var(--haze-space-md)` mobile, `var(--haze-space-lg)` desktop

No hex values in CSS — all colors via `var(--haze-color-*)` tokens.
  </action>
  <verify>
    <automated>cd "/Users/ostin/The Vault/电商project/haze-shopify-theme" && test -f sections/haze-ritual-description.liquid && test -f assets/haze-ritual-description.css && grep -q "haze-ritual-description.css.*asset_url.*stylesheet_tag" sections/haze-ritual-description.liquid && grep -q "haze-pdp-gallery.js" sections/haze-ritual-description.liquid && grep -q "{% schema %}" sections/haze-ritual-description.liquid && grep -q "| escape" sections/haze-ritual-description.liquid && grep -q "haze-ritual-description__heading" assets/haze-ritual-description.css && grep -q "haze-space-xl" assets/haze-ritual-description.css && grep -q "0.8125rem" assets/haze-ritual-description.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    Ritual description section renders centered editorial block: "The Ritual" overline in terracotta Label role, "A morning ritual worth doing." heading in Heading role, italic pull-quote, and body copy at 85% opacity. Schema allows editing all text fields via Customize panel. haze-pdp-gallery.js loads via deferred script tag. No hardcoded hex in CSS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ingredients section with SVG icons (Liquid + CSS + 4 SVGs)</name>
  <files>sections/haze-ingredients.liquid, assets/haze-ingredients.css, assets/haze-icon-silk.svg, assets/haze-icon-bristle.svg, assets/haze-icon-oeko.svg, assets/haze-icon-mulberry.svg</files>
  <read_first>
    - sections/haze-value-props.liquid (reference for icon block pattern — img tag, block.shopify_attributes, escape filter)
    - assets/haze-tokens.css (design tokens)
    - .planning/phases/03-pdp-supporting-pages/03-UI-SPEC.md (ingredient block anatomy, icon specs, grid layout)
  </read_first>
  <action>
**Create `sections/haze-ingredients.liquid`** — a 4-block responsive grid section (per D-14, D-15, D-16).

Follow the exact pattern from `sections/haze-value-props.liquid` — same icon loading approach (`img` tag with `asset_url`), same `block.shopify_attributes`, same `| escape` on text output.

```liquid
{{ 'haze-ingredients.css' | asset_url | stylesheet_tag }}

<section id="haze-ingredients-{{ section.id }}" class="haze-ingredients">
  {%- if section.settings.heading != blank -%}
    <h2 class="haze-ingredients__heading page-width">{{ section.settings.heading | escape }}</h2>
  {%- endif -%}
  <div class="haze-ingredients__grid page-width">
    {%- for block in section.blocks -%}
      {%- if block.type == 'ingredient' -%}
        <div class="haze-ingredients__block" {{ block.shopify_attributes }}>
          {%- if block.settings.icon != blank -%}
            <div class="haze-ingredients__icon" aria-hidden="true">
              <img
                src="{{ block.settings.icon | asset_url }}"
                alt=""
                width="40"
                height="40"
                loading="lazy"
              >
            </div>
          {%- endif -%}
          <h3 class="haze-ingredients__name">{{ block.settings.name | escape }}</h3>
          <p class="haze-ingredients__descriptor">{{ block.settings.descriptor | escape }}</p>
        </div>
      {%- endif -%}
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Haze Ingredients",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading",
      "default": "What makes it work."
    }
  ],
  "blocks": [
    {
      "type": "ingredient",
      "name": "Ingredient",
      "settings": [
        { "type": "text", "id": "icon", "label": "Icon filename (e.g. haze-icon-silk.svg)" },
        { "type": "text", "id": "name", "label": "Material name", "default": "Mulberry Silk" },
        { "type": "text", "id": "descriptor", "label": "Short descriptor", "default": "Gentle on every strand" }
      ]
    }
  ],
  "presets": [{
    "name": "Haze Ingredients",
    "blocks": [
      { "type": "ingredient", "settings": { "icon": "haze-icon-silk.svg", "name": "Mulberry Silk", "descriptor": "Gentle on every strand" }},
      { "type": "ingredient", "settings": { "icon": "haze-icon-bristle.svg", "name": "Boar Bristle", "descriptor": "Distributes natural oils" }},
      { "type": "ingredient", "settings": { "icon": "haze-icon-oeko.svg", "name": "OEKO-TEX Cotton", "descriptor": "Certified safe for skin" }},
      { "type": "ingredient", "settings": { "icon": "haze-icon-mulberry.svg", "name": "Silk Scrunchie", "descriptor": "Zero breakage at the tie" }}
    ]
  }]
}
{% endschema %}
```

**Create `assets/haze-ingredients.css`** with these exact rules:

```css
/* Haze Ingredients — 4-block responsive grid below ritual description */

.haze-ingredients {
  padding: var(--haze-space-xl) 0;
  background-color: var(--haze-color-cream);
}

.haze-ingredients__heading {
  font-family: var(--font-heading-family);
  font-size: 1.5rem;
  font-weight: 400;
  line-height: 1.2;
  text-align: center;
  color: var(--haze-color-dark);
  margin: 0 0 var(--haze-space-lg) 0;
}

.haze-ingredients__grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--haze-space-md);
}

@media screen and (min-width: 990px) {
  .haze-ingredients__grid {
    grid-template-columns: repeat(4, 1fr);
    gap: var(--haze-space-lg);
  }
}

.haze-ingredients__block {
  text-align: center;
  padding: var(--haze-space-md) 0;
}

.haze-ingredients__icon {
  margin-bottom: var(--haze-space-sm);
}

.haze-ingredients__icon img {
  width: 40px;
  height: 40px;
  display: inline-block;
}

.haze-ingredients__name {
  font-family: var(--font-heading-family);
  font-size: 1.5rem;
  font-weight: 400;
  line-height: 1.2;
  color: var(--haze-color-dark);
  margin: 0 0 var(--haze-space-xs) 0;
}

.haze-ingredients__descriptor {
  font-family: var(--font-body-family);
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.5;
  color: var(--haze-color-dark);
  opacity: 0.75;
  margin: 0;
}
```

Layout per UI-SPEC:
- Mobile (<990px): 2-column CSS grid, gap `var(--haze-space-md)` (16px)
- Desktop (990px+): 4-column CSS grid, gap `var(--haze-space-lg)` (32px)
- Section vertical padding: `var(--haze-space-xl)` (64px)
- Block vertical spacing: icon → name `var(--haze-space-sm)` (8px), name → descriptor `var(--haze-space-xs)` (4px)

**Create 4 SVG icon files** — all 40x40, stroke-only line art, `stroke="#C4714A"` (same pattern as Phase 2 value prop icons per D-16). stroke-width 1.5, no fill.

**`assets/haze-icon-silk.svg`** — Silk: wavy flowing lines representing smooth silk strands:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40" fill="none">
  <path d="M8 12C12 8 16 16 20 12C24 8 28 16 32 12" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M8 20C12 16 16 24 20 20C24 16 28 24 32 20" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M8 28C12 24 16 32 20 28C24 24 28 32 32 28" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
</svg>
```

**`assets/haze-icon-bristle.svg`** — Boar bristle brush: fan-shaped bristle silhouette:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40" fill="none">
  <path d="M20 34V22" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M20 22C20 22 10 18 8 10" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M20 22C20 22 14 16 14 8" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M20 22V6" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M20 22C20 22 26 16 26 8" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M20 22C20 22 30 18 32 10" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
</svg>
```

**`assets/haze-icon-oeko.svg`** — OEKO-TEX certification: leaf with a checkmark inside:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40" fill="none">
  <path d="M20 6C12 6 6 14 6 22C6 30 12 36 20 34C28 32 34 26 34 18C34 10 28 6 20 6Z" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
  <path d="M14 20L18 24L26 16" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
</svg>
```

**`assets/haze-icon-mulberry.svg`** — Silk scrunchie: ribbon loop / figure-eight shape:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 40 40" fill="none">
  <path d="M20 14C16 14 12 16 12 20C12 24 16 26 20 26C24 26 28 24 28 20C28 16 24 14 20 14Z" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M14 16C10 12 8 16 10 20C12 24 14 24 14 24" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M26 16C30 12 32 16 30 20C28 24 26 24 26 24" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M16 10C18 8 22 8 24 10" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
  <path d="M16 30C18 32 22 32 24 30" stroke="#C4714A" stroke-width="1.5" stroke-linecap="round"/>
</svg>
```

All 4 SVGs use: 40x40 viewBox, `fill="none"`, `stroke="#C4714A"`, `stroke-width="1.5"`, `stroke-linecap="round"`. This matches the Phase 2 value prop icon pattern exactly.
  </action>
  <verify>
    <automated>cd "/Users/ostin/The Vault/电商project/haze-shopify-theme" && test -f sections/haze-ingredients.liquid && test -f assets/haze-ingredients.css && test -f assets/haze-icon-silk.svg && test -f assets/haze-icon-bristle.svg && test -f assets/haze-icon-oeko.svg && test -f assets/haze-icon-mulberry.svg && grep -q "haze-ingredients.css.*asset_url.*stylesheet_tag" sections/haze-ingredients.liquid && grep -q "block.shopify_attributes" sections/haze-ingredients.liquid && grep -q "| escape" sections/haze-ingredients.liquid && grep -q "{% schema %}" sections/haze-ingredients.liquid && grep -q "repeat(4, 1fr)" assets/haze-ingredients.css && grep -q 'stroke="#C4714A"' assets/haze-icon-silk.svg && grep -q 'stroke="#C4714A"' assets/haze-icon-bristle.svg && grep -q 'stroke="#C4714A"' assets/haze-icon-oeko.svg && grep -q 'stroke="#C4714A"' assets/haze-icon-mulberry.svg && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    Ingredients section renders 4 icon blocks in a responsive grid (2-col mobile/tablet, 4-col desktop). Each block has a 40x40 terracotta-stroke SVG icon, Cormorant Garamond material name (Heading role), and DM Sans descriptor at 75% opacity. Schema allows adding/removing/editing ingredient blocks via Customize panel. 4 SVG icon files created matching Phase 2 stroke-art pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema settings to HTML | User-editable text (heading, overline, quote, body, material names, descriptors) rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | haze-ritual-description.liquid | mitigate | `| escape` filter on all `type: text` settings; `type: richtext` is sanitized by Shopify server-side. No raw HTML injection possible via Customize panel. |
| T-03-02 | Injection | haze-ingredients.liquid | mitigate | `| escape` filter on all text settings (name, descriptor, icon filename). Icon filename goes through `asset_url` filter which only resolves files in the assets directory — no path traversal possible. |
</threat_model>

<verification>
1. `sections/haze-ritual-description.liquid` exists with schema, CSS loading, and gallery JS loading
2. `assets/haze-ritual-description.css` has centered column (max-width 680px), correct typography roles
3. `sections/haze-ingredients.liquid` exists with block-based schema, icon loading via `asset_url`
4. `assets/haze-ingredients.css` has responsive grid (2-col mobile, 4-col desktop)
5. All 4 SVG icons exist with 40x40 viewBox, `stroke="#C4714A"`, `stroke-width="1.5"`, no fill
6. Both sections use `| escape` on text output and `block.shopify_attributes` on block elements
7. No hardcoded hex in CSS files — only design token custom properties
</verification>

<success_criteria>
- Both Liquid sections pass `shopify theme check` without errors
- CSS uses only `var(--haze-*)` and `var(--font-*)` custom properties (no raw hex)
- SVG icons render at 40x40 in terracotta color
- Sections are ready to be referenced by `templates/product.haze.json` in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/03-pdp-supporting-pages/03-01-SUMMARY.md`
</output>
